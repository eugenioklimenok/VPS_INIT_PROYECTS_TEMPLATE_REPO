# Config del framework

Configuracion base compartida del framework.

## Archivos

- `defaults.env`: defaults activos del repo
- `defaults.env.example`: ejemplo editable para futuras variantes

## Alcance en Fase 3

Esta carpeta centraliza defaults del bloque host para evitar valores hardcodeados en cada comando.
